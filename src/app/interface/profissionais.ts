export interface Psicologo {
  id: number;
  name: string;
  crp: string;
  specialty: string;
  is_online: boolean;
  is_presencial: boolean;
  created_at: string;
  modified_at: string;
  active: boolean;
}